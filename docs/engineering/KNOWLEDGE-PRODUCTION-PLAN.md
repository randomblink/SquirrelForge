# Knowledge Production Plan

## Purpose

This document is the working backlog and roadmap for `WP-ERROR` knowledge production under **Framework Baseline v2** (`SF-BASELINE-001`).

It is intentionally informal, mirroring `FRAMEWORK-OBSERVATIONS.md`'s own disclaimer:

It is not a specification. It is not governed by the engineering specification library. It is not reviewed under **SF-SPEC-005**. It is not versioned under **SF-SPEC-008**. It answers planning questions (which categories exist, which are planned, which is active, what depends on what) so that production has an explicit queue without turning the queue itself into governance.

It should be updated freely as production proceeds. It does not require engineering review to edit.

---

## 1. Current Catalog State

| Category | Entries | Baseline Certified | Taxonomy Document | Review |
|---|---|---|---|---|
| Database | 9 (`WP-ERROR-002`–`009`, `018`) | Yes | None (informal, disclosed gap) | `SF-REVIEW-033` |
| Filesystem | 3 (`WP-ERROR-016`, `019`, `020`) | Yes | `SF-TAXONOMY-001` | `SF-REVIEW-040` |
| REST API | 3 (`WP-ERROR-021`–`023`) | Yes | `SF-TAXONOMY-002` | `SF-REVIEW-192` (v3) |
| PHP Runtime | 2 (`WP-ERROR-014`, `015`) | Yes | None (informal, disclosed gap) | `SF-REVIEW-057` |
| Authentication | 4 (`WP-ERROR-024`–`027`) | Yes | `SF-TAXONOMY-003` | `SF-REVIEW-079` |
| Networking | 3 (`WP-ERROR-028`–`030`) | Yes | `SF-TAXONOMY-004` | `SF-REVIEW-088` |
| Bootstrap | 1 (`WP-ERROR-013`) | No — single-entry, degenerate for category review | None | — |
| Plugin | 3 (`WP-ERROR-017`, `031`, `032`) | Yes | `SF-TAXONOMY-005` | `SF-REVIEW-095` |
| Performance | 3 (`WP-ERROR-033`–`035`) | Yes | `SF-TAXONOMY-006` | `SF-REVIEW-104` |
| Media | 3 (`WP-ERROR-036`–`038`) | Yes (v3) | `SF-TAXONOMY-007` | `SF-REVIEW-158` |
| Theme | 2 (`WP-ERROR-039`–`040`) | Yes | `SF-TAXONOMY-008` | `SF-REVIEW-120` |
| CLI | 2 (`WP-ERROR-041`–`042`) | Yes | `SF-TAXONOMY-009` | `SF-REVIEW-127` |
| Cron | 2 (`WP-ERROR-043`–`044`) | Yes | `SF-TAXONOMY-010` | `SF-REVIEW-134` |
| Multisite | 1 (`WP-ERROR-045`) | Yes | `SF-TAXONOMY-011` | `SF-REVIEW-139` |
| Email | 2 (`WP-ERROR-046`–`047`) | Yes | `SF-TAXONOMY-012` | `SF-REVIEW-146` |

43 entries, 15 categories, 14 Baseline Certified, across the 14-specification framework at `SF-BASELINE-001`.

**Multisite versus Bootstrap, both single-entry — why one is certified and the other is not:** Bootstrap's own row above (`WP-ERROR-013` alone, "No — single-entry, degenerate for category review") predates this project's taxonomy-first methodology entirely; no taxonomy ever declared Bootstrap's planned entry set complete, so there is no evidence-based way to know whether `WP-ERROR-013` is genuinely all of Bootstrap or merely the one entry that happened to exist first. Multisite's own single-entry state is categorically different: `SF-TAXONOMY-011` performed the full proactive ownership sweep, examined four candidate second entries in genuine depth, and concluded — with independent verification in `SF-REVIEW-135` — that exactly one entry is the complete, deliberately-scoped category. A declared, evidence-based single-entry outcome is certifiable in a way an accidental one is not. Revisiting Bootstrap's own certification status under the current methodology remains a legitimate future option but is not undertaken here.

---

## 2. SF-SPEC-001 Approved Category Values (Current)

Per **SF-SPEC-001** Section 7, the approved list is: `Bootstrap`, `Configuration`, `PHP Runtime`, `Database`, `Filesystem`, `Plugin`, `Theme`, `REST API`, `Authentication`, `Security`, `Performance`, `Deployment`, `CLI`. That section's closing sentence — "No new category may be introduced without updating this specification" — makes this an exhaustive list, not merely illustrative examples, despite the "Examples:" heading.

Seven approved categories have **zero entries yet** and require no specification change to begin: `Configuration`, `Theme`, `Authentication`, `Security`, `Performance`, `Deployment`, `CLI`.

---

## 3. Candidate Roadmap

**Resolved:** all twelve candidates are now approved and startable with no further specification change. The project owner chose batch approval; `SF-SPEC-001` Version 1.2 (`SF-REVIEW-067`/`068`) added six category values in one revision rather than one at a time. The independent review renamed two of them for formatting consistency with the pre-existing list: `HTTP / Networking` → **`Networking`**, `Cron / Scheduled Tasks` → **`Cron`**. Use the exact approved strings (right-hand column) in any entry's own `**Category:**` field; the left-hand names remain this document's informal roadmap labels.

| Candidate (roadmap label) | Approved `SF-SPEC-001` category value | Status |
|---|---|---|
| Authentication & Authorization | `Authentication` | **Done** — Baseline Certified (`SF-REVIEW-079`) |
| Plugin Lifecycle | `Plugin` | **Done** — Baseline Certified (`SF-REVIEW-095`) |
| Theme Lifecycle | `Theme` | **Done** — Baseline Certified (`SF-REVIEW-120`) |
| Caching / Performance | `Performance` | **Done** — Baseline Certified (`SF-REVIEW-104`) |
| WP-CLI | `CLI` | **Done** — Baseline Certified (`SF-REVIEW-127`) |
| Filesystem & Updates (remaining gaps) | `Filesystem` | **Resolved — no post-certification change.** Researched and rejected; see the note below. |
| HTTP / Networking | `Networking` | **Done** — Baseline Certified (`SF-REVIEW-088`) |
| Media | `Media` | **Done** — Baseline Certified (`SF-REVIEW-113`) |
| Cron / Scheduled Tasks | `Cron` | **Done** — Baseline Certified (`SF-REVIEW-134`) |
| Email | `Email` | **Done** — Baseline Certified (`SF-REVIEW-146`) |
| Multisite | `Multisite` | **Done** — Baseline Certified (`SF-REVIEW-139`) |
| Site Health | `Site Health` | **Resolved — no taxonomy, zero entries planned.** Diagnostic lens, not a distinct failure domain; see the note below. |

**Site Health resolved as a diagnostic lens, not a failure category (2026-07-15):** The scoping question this table itself carried since Section 3 was first drafted — genuine failure domain, or a lens cutting across every other category — is now resolved without drafting a taxonomy, based on two independent lines of evidence. First, internal: six existing entries (`WP-ERROR-014`, `015`, `019`, `020`, `033`, `034`) already cite Site Health, and in every one of the six, it is cited purely as a *reporting surface* for a condition that entry already owns ("Site Health reports the running PHP version," "Site Health reporting a path as Not writable") — never as having its own independent failure mode, across six separate authoring sessions with no exception. Second, external: `WP_Site_Health` is opt-in, admin-triggered, and non-critical-path — no visitor-facing request depends on it, and it has no independent effect on site behavior; its entire purpose is to surface conditions that live elsewhere. One candidate for genuine, Site-Health-specific territory was examined and rejected: the Info tab's own directory-size calculation can be slow or exhaust the execution-time limit on a large site, a real, reported complaint — but the underlying failure (a generic PHP execution-time-limit exhaustion during a recursive directory traversal) is not specific to Site Health's own mechanism and would, if ever documented, belong to PHP Runtime or Performance (both already Baseline Certified) as a post-certification addition, not a new category. `Site Health` remains an approved `SF-SPEC-001` Section 7 category value; this resolution is a roadmap decision not to pursue it, not a specification change, and could be revisited under **SF-SPEC-013** Section 5.6-equivalent reasoning if future evidence surfaces a genuine, Site-Health-specific mechanism this analysis missed.

**Filesystem post-certification change researched and rejected (2026-07-15):** Reopening an already-`Baseline Certified` category carries a deliberately higher evidentiary bar than a fresh-start taxonomy, per **SF-SPEC-013** Section 5.6 — a new entry needs evidence of a genuinely distinct failure mechanism, not merely a plausible-sounding gap. Several candidates were checked directly against `WP-ERROR-019`'s own current text and found already covered by name: symlink handling, read-only mounts, and SELinux/AppArmor access-control layers are all already explicit within its own Scope and Diagnosis sections. The one candidate that survived that first pass — a cross-device (`EXDEV`) `rename()` failure, since `WP_Filesystem_Direct::move()`'s own copy-fallback is documented in its own source comment as "only... for single files," meaning a directory move across a mount boundary (a real, increasingly common topology when `wp-content/uploads` sits on network-mounted storage separate from a local staging directory) could fail outright — was resolved by reading WordPress core's own actual source (`wordpress-develop`, `trunk`) rather than relying on assumed behavior. `move_dir()` (`wp-admin/includes/file.php`) was added specifically to close this exact gap, with a proper `copy_dir()`-based recursive-copy fallback for directories; `class-wp-upgrader.php`'s own `install_package()` — the real file-swap step `WP-ERROR-032`/`040` already document — calls `move_dir()`, not the unsafe, fallback-less `move()` directly. Every plausible residual failure once `move_dir()`'s own fallback engages (a slower copy contributing to execution-time-limit exhaustion, a partial copy/delete failure) was traced to a cause `WP-ERROR-019`/`020`/`032`/`040` already document, not a new one. The only place the unsafe API would actually bite is a plugin choosing to call `move()` directly instead of `move_dir()` for a directory — a plugin's own coding choice, not a WordPress-core mechanism failure, the same class of condition this catalog has excluded throughout. `SF-TAXONOMY-001` and the Filesystem Knowledge Baseline are unchanged by this research; it remains a candidate to revisit if a genuinely new mechanism is identified in the future, per **SF-SPEC-013** Section 5.6.

With Site Health and the Filesystem post-certification question both resolved, every approved `SF-SPEC-001` category value is now either Baseline Certified or explicitly resolved as out of scope. The candidate-category roadmap this section originally opened with is complete.

---

## 4. Suggested Prioritization

Ordered by estimated operational value — how often the condition is likely to be the actual cause of a real, reported WordPress problem — not alphabetically or by ease of authoring. This is a starting proposal, not a committed order; it should be adjusted against real production/support data as it accumulates (Section 7).

All twelve are now approved (Section 3); this ordering is unaffected by that resolution.

1. **Authentication & Authorization** (`Authentication`) — ready now; login/access failures are among the most common real-world WordPress support issues, and the category already borders `WP-ERROR-022` (REST API Access Denied), so the boundary work is partly done.
2. **Plugin Lifecycle** (`Plugin`) — ready now; plugin activation/conflict failures are extremely common and the category already has one entry (`WP-ERROR-017`) establishing precedent.
3. **Networking** (`Networking`) — highest-value remaining category; outbound API calls, SSL, DNS, and reverse-proxy failures are a frequent, currently uncovered failure surface.
4. **Caching / Performance** (`Performance`) — ready now; borders `WP-ERROR-009` (Database Query Timeout), so needs an explicit boundary statement in its own taxonomy.
5. **Media** (`Media`) — common failure surface (uploads, image processing); borders `WP-ERROR-014` (PHP extension) and Filesystem.
6. **Theme Lifecycle** (`Theme`) — ready now; lower volume than Plugin Lifecycle but a clear, well-bounded domain.
7. **Email** (`Email`) — common but frequently a downstream symptom of Networking or Configuration failures rather than its own root cause; worth sequencing after Networking so the boundary is easier to draw.
8. **Cron** (`Cron`) — real but often silent-failure territory, lower reported-incident volume.
9. **Filesystem & Updates (remaining gaps)** (`Filesystem`) — valuable but procedurally heavier (post-certification change to an already-certified category); sequence once the team has practiced the post-certification process on paper via **SF-SPEC-013** Section 5.6.
10. **WP-CLI** (`CLI`) — ready now; developer-facing rather than production-incident-driven, lower priority.
11. **Site Health** (`Site Health`) — resolve the "domain vs. diagnostic lens" design question (Section 3) before taxonomy work; likely lower urgency.
12. **Multisite** (`Multisite`) — specialized, smaller installed base; lowest priority absent a specific signal it's needed sooner.

---

## 5. Category Completion Criteria (Template)

Unchanged from the process **SF-SPEC-013** already governs; restated here only as a planning checklist, not a new requirement:

1. Category value approved in **SF-SPEC-001** Section 7, if not already present.
2. `SF-TAXONOMY-XXX` authored: category boundary, planned entries with one-line ownership each, rejected candidates with reasoning (**SF-SPEC-013** Section 5.1).
3. Each planned entry authored and independently reviewed to Production Ready (Section 5.2, 5.3).
4. Category-level consistency review (Section 5.4's sibling criteria: no overlap, cross-reference symmetry, consistent recovery guidance).
5. Category-level baseline certification review (Section 5.4, Section 8 checklist) — `Baseline Certified` designation.
6. `scripts/validate-repo.sh` clean before and after.

---

## 6. Dependencies and Boundary Risks

Flagged in advance so taxonomy authors design the boundary deliberately rather than discovering the overlap during a later consistency review:

* **HTTP / Networking vs. REST API** — REST API already owns "request accepted, callback executing" territory (`WP-ERROR-021`–`023`); HTTP/Networking should own the outbound/lower-layer transport conditions (DNS, TLS, timeouts, reverse proxies) rather than re-litigating inbound REST request handling.
* **Authentication & Authorization vs. REST API** — `WP-ERROR-022` (REST API Access Denied) already covers authentication/authorization failure *within* a REST request. A general Authentication category needs an explicit boundary statement distinguishing site-wide login/session/capability failures from that REST-specific case, the same way `WP-ERROR-014`/`015` had to disambiguate PHP Runtime from Bootstrap.
* **Caching / Performance vs. Database** — `WP-ERROR-009` (Database Query Timeout) already covers one performance-adjacent condition. Caching/Performance should own application- and object-cache-layer conditions, not re-absorb database-specific timeouts.
* **Media vs. Filesystem vs. PHP Runtime** — media failures often manifest as filesystem permission issues (`WP-ERROR-019`/`020`) or missing PHP extensions (`WP-ERROR-014`, e.g. `gd`/`imagick`). Media's own taxonomy should explicitly defer to those existing entries for that subset and scope itself to media-specific conditions (format/codec handling, thumbnail generation logic) instead.
* **Site Health** — before authoring, decide whether it is a distinct failure domain (Site Health's own reporting logic malfunctioning) or a diagnostic lens referencing every other category's own conditions. The latter would not need its own `WP-ERROR` entries at all.

---

## 7. Evidence Log

Empty scaffold. Per the project owner's own direction, this is the evidence base a future Framework Baseline v3 would need to justify reopening the specification layer — not a place to record routine entry authoring, but a place to note when production repeatedly exposes the *same* governance deficiency (as opposed to a one-off inconvenience).

| Date | Category / Entry | Observation | Governance Deficiency Suspected? |
|---|---|---|---|
| — | — | — | — |

---

## 8. Active Category

No category is currently active. Email is complete and Baseline Certified (`SF-REVIEW-146`) — the fourteenth category to reach that designation, the eleventh (after REST API, Authentication, Networking, Plugin, Performance, Media, Theme, CLI, Cron, and Multisite) built from a dedicated taxonomy from the outset, and the seventh consecutive category whose complete planned-entry set was produced under a single, unrevised taxonomy boundary. Category Work Order: `SF-TAXONOMY-012` (Email Error Taxonomy), Version 1.2, independently reviewed per `SF-REVIEW-140` (Approved). The proactive ownership sweep found this category's own territory entirely unclaimed, and established the central technical finding that PHPMailer's SMTP transport uses its own direct socket-level client, not `WP_Http` — genuinely distinct from `WP-ERROR-028`/`029` despite superficial similarity. Also discovered and resolved a cross-taxonomy dependency: `SF-TAXONOMY-003`'s own deferred password-reset candidate explicitly anticipated needing Email category boundaries to disambiguate its own delivery-failure portion, now resolved here. Entries: `WP-ERROR-046` (Local Mail Transport Failure), `WP-ERROR-047` (SMTP Mail Transport Failure), both Production Ready.

- `WP-ERROR-046` (`SF-REVIEW-141`/`142`) — drafted directly from `SF-TAXONOMY-012`'s own declared scope. Reasons Critical severity from the range of what depends on email delivery plus PHP `mail()`'s own well-documented weak success signal — `wp_mail_failed` structurally cannot fire for the "mail() returned true but the MTA silently failed" case, since PHPMailer has no visibility into anything after `mail()` returns. Both reviews returned zero findings, a clean pass explicitly confirmed as valid rather than manufactured. `SF-REVIEW-142` explicitly and deliberately deferred correcting `WP-ERROR-024`'s own stale cross-reference until `WP-ERROR-047` also existed, tracked rather than silently dropped.
- `WP-ERROR-047` (`SF-REVIEW-143`/`144`) — the second and final entry, drawing a boundary against `WP-ERROR-028`/`029` resting entirely on mechanism (PHPMailer's own direct socket-level SMTP client, not `WP_Http`) rather than any observable symptom difference — the first such boundary in this catalog. Explicitly contrasts its own materially better diagnostic visibility with `WP-ERROR-046`'s own silence, reasoned in the opposite direction rather than copied. Independent review closed the deferral `SF-REVIEW-142` had tracked: `WP-ERROR-024`'s own citation updated to name both transport entries together.
- **Consistency review** (`SF-REVIEW-145`) treated both as a system: confirmed the two-entry partition holds, cross-reference symmetry is intact, and the deliberate severity/visibility contrast between the two entries is genuinely reasoned rather than two non-contradictory assertions. Zero findings requiring correction.
- **Baseline certification** (`SF-REVIEW-146`) required no correction of its own, since the consistency review had already found nothing requiring one.

Multisite is complete and Baseline Certified (`SF-REVIEW-139`) — the thirteenth category to reach that designation, the tenth (after REST API, Authentication, Networking, Plugin, Performance, Media, Theme, CLI, and Cron) built from a dedicated taxonomy from the outset, the sixth consecutive category whose complete planned-entry set was produced under a single, unrevised taxonomy boundary, and the first single-entry category ever certified (see Section 1's own note distinguishing this from Bootstrap's own uncertified single-entry state). Category Work Order: `SF-TAXONOMY-011` (Multisite Error Taxonomy), Version 1.1, independently reviewed per `SF-REVIEW-135` (Approved). Resolves `SF-TAXONOMY-009` Section 2's own explicit forward-reference to Multisite as the owner of WordPress's own site-resolution mechanism. The proactive ownership sweep found this category's own adjacent territory already extensively claimed across nine existing entries (`WP-ERROR-004`/`005`/`006`/`017`/`024`/`026`/`036`/`037`/`042`), narrowing this category to a single genuinely novel mechanism. Entry: `WP-ERROR-045` (Multisite Site Resolution Failure), Production Ready.

- `WP-ERROR-045` (`SF-REVIEW-136`/`137`) — drafted directly from `SF-TAXONOMY-011`'s own declared scope, resolving the territory `WP-ERROR-042` explicitly reserved. Deliberately consolidates two manifestations (no site resolved, wrong site resolved) on the same precedent established for `WP-ERROR-043`. Reasons Critical severity from the wrong-site-resolved manifestation's own data-isolation risk on a multi-tenant network. Carries six Distinction citations, each independently confirmed necessary given how extensively multisite behavior was already woven into existing entries. Independent review caught one real finding: `WP-ERROR-042`'s own three "future Multisite category" forward-references, written before this entry existed, converted to a real citation of it — mirroring the taxonomy-level forward-reference resolutions already established (Media resolving Filesystem's promise, Theme resolving Plugin's promise).
- **Consistency review** (`SF-REVIEW-138`) — the first for a single-entry category, adapting its own scope to emphasize external consistency (forward-reference closure, Distinction-citation accuracy) since there is no sibling entry for internal symmetry checks. Zero findings requiring correction.
- **Baseline certification** (`SF-REVIEW-139`) — the first for a single-entry category, applying the mutual-exclusivity and cross-reference criteria in their degenerate single-entry form. Required no correction of its own.

Cron is complete and Baseline Certified (`SF-REVIEW-134`) — the twelfth category to reach that designation, and the ninth (after REST API, Authentication, Networking, Plugin, Performance, Media, Theme, and CLI) built from a dedicated taxonomy from the outset — and the fifth consecutive category (after Performance, Media, Theme, CLI) whose complete planned-entry set was produced under a single, unrevised taxonomy boundary. Category Work Order: `SF-TAXONOMY-010` (Cron Error Taxonomy), Version 1.2, independently reviewed per `SF-REVIEW-128` (Approved). The proactive ownership sweep found `WP-ERROR-013`/`028`/`009`/`014`/`015` and `SF-TAXONOMY-005` already correctly scoped around WP-Cron without claiming its own core triggering or event-processing mechanism, requiring no boundary correction. Plans two entries dividing by processing stage, sequentially gated: `WP-ERROR-043` (Scheduled Cron Event Not Triggered) and `WP-ERROR-044` (Scheduled Cron Event Callback Failure), both Production Ready.

- `WP-ERROR-043` (`SF-REVIEW-129`/`130`) — drafted directly from `SF-TAXONOMY-010`'s own declared scope. Deliberately consolidates four causes that present identically to an observer ("my scheduled events aren't running"): absence of qualifying site traffic, the loopback trigger request failing to connect, `DISABLE_WP_CRON` set without a working replacement, and a stuck/contended internal cron lock. Reasons Critical severity from the wide range of what can be scheduled plus the characteristic invisibility of every one of its causes. Independent review caught one real finding: `WP-ERROR-028`'s own Distinction section didn't yet name the loopback-trigger hand-off relationship or its own diagnostic asymmetry (the loopback request is non-blocking and never inspected, unlike `WP-ERROR-028`'s own inspectable `WP_Error`) — added.
- `WP-ERROR-044` (`SF-REVIEW-131`/`132`) — the second and final entry: once triggering succeeds, a specific event's own callback can still fail via a post-bootstrap fatal error, execution-time exhaustion, or a missing hook registration. Documents a "blast radius" risk unique to this entry — because `wp_cron()` processes every due event within one shared process and execution-time budget, a severe failure in one callback can silently prevent other, unrelated due events in the same pass from running at all. Independent review caught one real finding: `WP-ERROR-013`'s own post-bootstrap illustrative example list (plugin callback, REST route handler, theme template rendering) didn't yet name a scheduled cron event's own callback — extended.
- **Consistency review** (`SF-REVIEW-133`) treated both as a system: confirmed the two-entry partition holds, cross-reference symmetry is intact, and the shared Critical severity is independently and correctly reasoned for each entry rather than inherited. Zero findings requiring correction. Also updated `FRAMEWORK-OBSERVATIONS.md`'s hub-entry observation with this cycle's two new data points, confirming `WP-ERROR-028` as a third hub entry alongside `WP-ERROR-013`/`014`/`015` — the pattern isn't specific to those three, but applies to any general-condition hub for a given failure class. Perfect catch rate (8 for 8) continues to support "monitor, don't change process."
- **Baseline certification** (`SF-REVIEW-134`) required no correction of its own, since the consistency review had already found nothing requiring one.

CLI is complete and Baseline Certified (`SF-REVIEW-127`) — the eleventh category to reach that designation, and the eighth (after REST API, Authentication, Networking, Plugin, Performance, Media, and Theme) built from a dedicated taxonomy from the outset — and the fourth consecutive category (after Performance, Media, Theme) whose complete planned-entry set was produced under a single, unrevised taxonomy boundary. It is also the first Baseline Certified category whose execution context is not an HTTP request at all, and the first whose two entries deliberately carry different severity classifications (Low, Critical) for structurally related conditions. Category Work Order: `SF-TAXONOMY-009` (CLI Error Taxonomy), Version 1.2, independently reviewed per `SF-REVIEW-121` (Approved). The proactive ownership sweep found the overwhelming majority of candidate "WP-CLI failure" conditions already owned by more than a dozen existing entries that explicitly name WP-CLI as an alternate entry point, narrowing this category to two genuinely novel mechanisms dividing by a "sequential-but-conditional" model new to this catalog. Entries: `WP-ERROR-041` (WP-CLI Cannot Locate a WordPress Installation), `WP-ERROR-042` (WP-CLI Multisite Site Context Resolution Failure), both Production Ready.

- `WP-ERROR-041` (`SF-REVIEW-122`/`123`) — drafted directly from `SF-TAXONOMY-009`'s own declared scope. Documents WP-CLI's own separate, pre-bootstrap directory-tree search failing to identify a WordPress installation, distinct from `wp-load.php`'s own simpler check (the not-yet-authored `WP-ERROR-010`'s own conceptual territory) and from any bootstrap-sequence fatal error once `wp-load.php` is actually reached (`WP-ERROR-013`'s own territory). The first entry in this catalog reasoned to Low severity from first principles — a deliberate departure below the High/High exceptions previously used (`WP-ERROR-009`/`034`/`036`/`037`) — since this condition has no plausible path to any site-facing impact at all. Independent review caught one real finding: `WP-ERROR-013`'s own Distinction section didn't yet distinguish WP-CLI's own pre-bootstrap discovery failure from `WP-ERROR-010`'s own conceptual territory — added, the fifth confirmed instance of this catalog's own hub-entry cross-reference pattern.
- `WP-ERROR-042` (`SF-REVIEW-124`/`125`) — the second and final entry, drafted as the sibling of `WP-ERROR-041`'s installation-discovery stage: WP-CLI's own failure to establish which site a command should target on a Multisite installation, since it has no HTTP Host header to derive one from automatically. Reasons Critical severity (range-based) explicitly, in deliberate contrast with `WP-ERROR-041`'s own Low classification — its silent-misresolution manifestation (WP-CLI's own actual default behavior when `--url` is omitted: quietly operating against the network's primary site) can produce genuine, silent data loss on a live, unintended site, a risk `WP-ERROR-041`'s own condition categorically cannot reach. Independent review caught one real finding: `WP-ERROR-041`'s own forward citation to this entry had gone stale once it was authored, caught by `validate-repo.sh` Check A — corrected to a real link.
- **Consistency review** (`SF-REVIEW-126`) treated both as a system: confirmed the two-entry partition holds, cross-reference symmetry is intact, and specifically scrutinized the deliberate Low/Critical severity divergence, confirming it a genuine, substantiated distinction rather than an unexplained inconsistency. Zero findings requiring correction.
- **Baseline certification** (`SF-REVIEW-127`) required no correction of its own, since the consistency review had already found nothing requiring one.

Theme is complete and Baseline Certified (`SF-REVIEW-120`) — the tenth category to reach that designation, and the seventh (after REST API, Authentication, Networking, Plugin, Performance, and Media) built from a dedicated taxonomy from the outset — and the third consecutive category (after Performance, Media) whose complete planned-entry set was produced under a single, unrevised taxonomy boundary. Category Work Order: `SF-TAXONOMY-008` (Theme Lifecycle Error Taxonomy), Version 1.2, independently reviewed per `SF-REVIEW-114` (Approved). Resolves `SF-TAXONOMY-005` Section 2's own explicit forward-reference to Theme as "an exact structural parallel WordPress itself maintains." The proactive ownership sweep found `WP-ERROR-013`/`019`/`020`/`028`/`029`/`014`/`015` already correctly scoped to include theme alongside plugin wherever the parallel mechanism applies, requiring no boundary correction — the fourth consecutive taxonomy (after Plugin's own mid-production correction, then Performance and Media cleanly) to pass the sweep during drafting. Entries: `WP-ERROR-039` (Theme Activation/Switching Failure), `WP-ERROR-040` (Theme Update Failure), both Production Ready, narrower than Plugin's three-stage model since WordPress has no must-use-theme-equivalent mechanism.

- `WP-ERROR-039` (`SF-REVIEW-115`/`116`) — drafted directly from `SF-TAXONOMY-008`'s own declared scope. Its own central technical finding: WordPress's `switch_theme()` commits the `template`/`stylesheet` options *before* firing `after_switch_theme`, unlike `WP-ERROR-031`'s own plugin-activation mechanism, which sandboxes a pre-flight check specifically to avoid ever marking a fatally-broken plugin active. This makes a callback failure at that third cause genuinely more severe than any comparable Plugin-category cause — reasoned explicitly rather than inherited by analogy. Independent review caught two real findings, both cross-document completeness gaps: `WP-ERROR-014`/`015`'s own Typical Symptoms sections cited only `WP-ERROR-031` for a requirements-blocked activation notice (extended to also cite `WP-ERROR-039`), and `WP-ERROR-013`'s own Common Causes list had no theme-switch-specific mechanism bullet (added). `WP-ERROR-039` itself required no correction; `SF-TAXONOMY-008` required no revision.
- `WP-ERROR-040` (`SF-REVIEW-117`/`118`) — the second and final entry, drafted as the direct structural parallel to `WP-ERROR-032`, sharing `Theme_Upgrader`'s own `WP_Upgrader` base class and the `wp-content/upgrade` staging directory with `Plugin_Upgrader`. Reasons explicitly, rather than by blind analogy, that its own worst-case path (an interrupted update leaving the currently-active theme's files inconsistent) is reached more routinely in practice than Plugin's equivalent, since a theme update commonly targets the site's one active theme — while carefully avoiding a claim that the mechanism itself is more dangerous in kind. Independent review caught one real finding: `WP-ERROR-013`'s own Common Causes list had bullets for the `WP-ERROR-032` and `WP-ERROR-039` downstream-fatal mechanisms but none yet for this entry's own distinct interrupted-update mechanism — added. `WP-ERROR-040` itself required no correction.
- **Consistency review** (`SF-REVIEW-119`) treated both as a system: confirmed the two-stage partition holds, cross-reference symmetry is intact, and found one Minor finding — `WP-ERROR-032`'s own Scope exclusion named "Theme update failures" without a link, now that `WP-ERROR-040` actually claims that condition; corrected. Also confirmed two generic "Theme category" taxonomy-level mentions correctly remain unedited, per this catalog's established practice for frozen taxonomy documents.
- **Baseline certification** (`SF-REVIEW-120`) required no correction of its own, since the consistency review had already caught what would otherwise have surfaced here.

Media is complete and Baseline Certified (`SF-REVIEW-113`) — the ninth category to reach that designation, and the sixth (after REST API, Authentication, Networking, Plugin, and Performance) built from a dedicated taxonomy from the outset — and the second consecutive category (after Performance) whose complete planned-entry set was produced under a single, unrevised taxonomy boundary. Category Work Order: `SF-TAXONOMY-007` (Media Error Taxonomy), Version 1.3, independently reviewed per `SF-REVIEW-105` (Approved). Resolves `SF-TAXONOMY-001`'s own explicit forward-reference to a future "dedicated Media/Uploads category." Research (the proactive cross-category ownership sweep, now standard since `SF-TAXONOMY-006`) found two genuine, previously-unclaimed gaps: PHP/WordPress upload-size-limit rejection (already disclosed and excluded by `WP-ERROR-020`, but never claimed by any entry) and WordPress's own file-type/MIME validation gate (never mentioned anywhere). Entries: `WP-ERROR-036` (Upload Size Limit Exceeded), `WP-ERROR-037` (Upload File Type Rejected), `WP-ERROR-038` (Image Processing Failure), all Production Ready, structured as a sequential pipeline rather than independent mechanisms.

- `WP-ERROR-036` (`SF-REVIEW-106`/`107`) — drafted directly from `SF-TAXONOMY-007`'s own declared scope. Keeps three causes distinct: `post_max_size` exceeded (WordPress's own code still executes, but with empty `$_POST`/`$_FILES` and no PHP-level error code — the most diagnostically difficult case), `upload_max_filesize` exceeded (PHP populates `UPLOAD_ERR_INI_SIZE`, which WordPress translates), and WordPress's own further-restricted `wp_max_upload_size()` (a userland check after PHP has already accepted the file). Independent review caught three real Minor findings: an overstated description of PHP's own request-handling behavior under `post_max_size` (corrected — WordPress's code does execute, it's not skipped), a missing diagnostic signal (PHP's own error-log warning, a genuine way to distinguish this entry's own condition from an excluded web-server-level size limit, added), and a cross-document completeness gap in `WP-ERROR-020`'s own pre-existing exclusion bullet, which described this entry's own condition without citing it (added). `SF-TAXONOMY-007` itself required no revision.
- `WP-ERROR-037` (`SF-REVIEW-108`/`109`) — the second consecutive entry in this category to reach Production Ready without a taxonomy revision. Keeps two causes distinct: an extension absent from the currently-applicable allowed-types list (itself sensitive to multisite status and the uploading user's own `unfiltered_upload` capability), and an extension that passes but whose actual content, verified via `fileinfo`/image-detection where available, doesn't match. Explicitly frames the rejection as the gate's own designed, correctly-functioning behavior — not a malfunction — and declines to extend that framing into ownership of a broader security-incident investigation, consistent with how this catalog treats the still-taxonomy-less Security category elsewhere. Independent review caught one real finding: the entry's own `unfiltered_upload` coverage omitted `DISALLOW_UNFILTERED_UPLOADS`, a real `wp-config.php` constant that revokes the capability regardless of role — added.
- `WP-ERROR-038` (`SF-REVIEW-110`/`111`) — the third and final entry, and the third consecutive entry in this category to reach Production Ready without a taxonomy revision. Reasons Severity (Critical, range-based) from first principles, departing deliberately from its own two Media siblings' High/High classification, since decoding a large source image can genuinely exhaust PHP memory and produce a fatal error. Proactively drew the same categorical-versus-observable boundary against `WP-ERROR-014` that `WP-ERROR-029` already established, since `WP-ERROR-014`'s own diagnosis text names "a `gd` build without a specific image format" as its own territory. Independent review caught one real finding: an illustrative example (an animated GIF losing animation during resizing) was stated as a fixed, universal behavior rather than editor-/version-dependent — corrected.
- **Consistency review** (`SF-REVIEW-112`) treated all three as a system: confirmed the sequential-pipeline partition holds exactly, confirmed `WP-ERROR-037`'s "graceful degradation" and `WP-ERROR-038`'s "categorical-versus-observable hand-off" treatments of the `WP-ERROR-014` boundary are genuinely different, both-correct situations rather than an inconsistency, and found one Minor finding — `WP-ERROR-038`'s own Related Errors wording standardized, the fourth individual instance of that defect class across the catalog (after `WP-ERROR-017`, `031`, `035`). That fourth instance crossed this project's own evidentiary threshold for acting rather than continuing to disclose: `scripts/validate-repo.sh` gained a Check D comparing every entry's own Related Errors intro sentence against the catalog's majority wording, catching the drift mechanically at commit time going forward.
- **Baseline certification** (`SF-REVIEW-113`) required no correction of its own, since the consistency review had already caught what would otherwise have surfaced here.

Caching / Performance is complete and Baseline Certified (`SF-REVIEW-104`) — the eighth category to reach that designation, and the fifth (after REST API, Authentication, Networking, and Plugin) built from a dedicated taxonomy from the outset — and the first whose complete planned-entry set was produced under a single, unrevised taxonomy boundary after exactly one pre-authoring correction. Category Work Order: `SF-TAXONOMY-006` (Caching / Performance Error Taxonomy), Version 1.3, independently reviewed per `SF-REVIEW-096` (Approved). Drafting this taxonomy required more cross-entry research than any prior one: four existing, Production-Ready entries (`WP-ERROR-021`, `025`, `027`, `030`) already fully own a specific caching-related symptom within their own domains, so this category's own territory is narrow — the caching *mechanisms'* own operational state, not any downstream content served incorrectly. Entries: `WP-ERROR-033` (Persistent Object Cache Backend Unavailable), `WP-ERROR-034` (Page Cache Not Active), `WP-ERROR-035` (OPcache Stale Bytecode), all Production Ready.

- `WP-ERROR-033` (`SF-REVIEW-097`/`098`) — drafted directly from `SF-TAXONOMY-006`'s own declared scope, per explicit project-owner direction, as the first direct test of whether that taxonomy's own proactive cross-category ownership sweep (performed during its own drafting, unlike `SF-TAXONOMY-005`'s reactive mid-production correction) actually prevents the defect class `WP-ERROR-032` exposed. It did: this entry required no taxonomy revision. Keeps three causes distinct — connection failure, operation failure, and mechanism-level initialization failure — and maintains explicit evidence-quality layering throughout (WordPress core's own behavior versus the `object-cache.php` drop-in's own behavior versus the specific backend's own behavior), preserving the correction `SF-REVIEW-096` (IF-1) already applied at the taxonomy level rather than reintroducing a WordPress-core-level fallback guarantee that doesn't exist. Independent review caught two real Minor findings: a technically imprecise description of Redis's own `maxmemory`/eviction rejection behavior (corrected within this entry), and a cross-document gap in `WP-ERROR-009`'s own Common Causes list, which didn't name an object-cache outage as a contributing factor to increased query load (added, cross-referencing this entry).
- `WP-ERROR-034` (`SF-REVIEW-099`/`100`) — the second consecutive entry in this category to reach Production Ready without a taxonomy revision. Deliberately classified High/High rather than the catalog's usual range-based Critical, mirroring `WP-ERROR-009`'s own considered exception (no plausible manifestation includes a total loss of a functioning request path) rather than `WP-ERROR-033`'s own opposite departure (whose own worst case can include a fatal-erroring drop-in). States explicitly that WordPress core provides no page-caching implementation of its own at all — a stronger asymmetry than the object cache's own core-level fallback — maintaining the same evidence-quality discipline `WP-ERROR-033` established. Independent review caught one real finding: the claim that every one of this entry's three causes is "functionally identical to no caching plugin installed" overstated cause 3 specifically, where a drop-in's own repeated, failing write attempts can impose more overhead than simple inactivity — corrected. Independently re-applied the `SF-REVIEW-092` cross-reference precedent to `WP-ERROR-020`'s own pre-existing "page-cache/object-cache plugin" mention, confirming no update was required (illustrative context for that entry's own condition, not a competing claim).
- `WP-ERROR-035` (`SF-REVIEW-101`/`102`) — the third and final entry, and the third consecutive entry in this category to reach Production Ready without a taxonomy revision. Reasons Severity (Critical, range-based) from first principles rather than inherited from either sibling: OPcache's own per-file, non-atomic invalidation can produce a genuine PHP fatal error from cross-file bytecode inconsistency, not merely a performance degradation. Draws an explicit boundary against `WP-ERROR-032` (files-on-disk correctness versus PHP's own execution of them) and states plainly that this entry concerns cached *compiled code*, not cached *data or content* — the axis distinguishing it from every other entry in this category and several entries in other categories. Independent review caught two real findings: Section 8 didn't warn that a CLI-invoked `opcache_reset()` (including via WP-CLI) commonly resets a different, separate OPcache instance than the one serving web requests — a common, practically important operational trap, corrected; and `WP-ERROR-032`'s own "plugin's own new-version code defect" exclusion didn't distinguish a genuine code defect from this entry's own condition, both presenting as "the update succeeded but something still seems wrong" — extended to name this entry.
- **Consistency review** (`SF-REVIEW-103`) treated all three as a system: confirmed the three-mechanism partition holds exactly, and found two Minor findings — `WP-ERROR-035`'s own Related Errors wording standardized, and `WP-ERROR-013`'s own Common Causes list extended a third time (after `WP-ERROR-031`, `032`) for a newly-documented, fatal-error-capable cause. Directly assessed the project owner's own evidentiary question: whether the proactive ownership sweep introduced after `WP-ERROR-032` eliminates the review-scope defect class `SF-TAXONOMY-005` exhibited. Concluded yes, now supported by a taxonomy requiring exactly one pre-authoring correction followed by three consecutive clean entries and a clean consistency review.
- **Baseline certification** (`SF-REVIEW-104`) required no correction of its own, since the consistency review had already caught what would otherwise have surfaced here.

Plugin Lifecycle is complete and Baseline Certified (`SF-REVIEW-095`) — the seventh category to reach that designation, and the fourth (after REST API, Authentication, and Networking) built from a dedicated taxonomy document — and the first whose taxonomy was declared *after* an existing, unbaselined entry (`WP-ERROR-017`) already occupied part of the category, and required a mid-production correction (`SF-TAXONOMY-005` v1.1→v1.2) before its final entry was authored. Category Work Order: `SF-TAXONOMY-005` (Plugin Lifecycle Error Taxonomy), Version 1.3. Entries: `WP-ERROR-017` (Must-Use Plugin Fatal Error), `WP-ERROR-031` (Plugin Activation Failure), `WP-ERROR-032` (Plugin Update Failure), all Production Ready.

- `WP-ERROR-031` (`SF-REVIEW-090`/`091`) — drafted directly from `SF-TAXONOMY-005`'s own declared scope, per explicit project-owner direction, as a deliberate test of whether the taxonomy is complete enough to support entry authoring without a fresh boundary discussion. Keeps three causes distinct: WordPress's own native pre-activation requirement gate (`Requires PHP`/`Requires at least`/`Requires Plugins`, refused before any plugin code runs), an activation-time fatal error during the plugin's own file include (WordPress's own built-in protection, distinguished explicitly from a generic PHP-runtime fatal error), and the plugin's own `register_activation_hook()` callback failing or self-halting. Diagnosis starts from confirming activation actually failed and capturing WordPress's own exact message, before narrowing to which of the three mechanisms is responsible. Hands off to `WP-ERROR-014`/`015` for extension/version root causes rather than duplicating their own diagnostic content. Independent review caught one real finding, outside this entry's own text: `WP-ERROR-017`'s own "ordinary plugin activation/deactivation" exclusion bullet described this entry's own territory without citing it — corrected. `SF-TAXONOMY-005` itself required no revision to support this entry, satisfying the project owner's own stated completeness test.
- `WP-ERROR-032` (`SF-REVIEW-092`/`093`) — required a pre-authoring taxonomy correction (`SF-TAXONOMY-005` v1.1→v1.2): research surfaced two real, previously unaddressed overlaps `SF-REVIEW-089` had not caught — `WP-ERROR-019`/`020` (Filesystem) already explicitly claim the `wp-content/upgrade` staging directory's own permission/capacity dimension of an update failure, and `WP-ERROR-028`/`029` (Networking) already explicitly claim the update package's own download-connection/TLS dimension. Narrowed the entry to the update *mechanism's* own process (pre-update compatibility gate, package integrity/extraction, non-permission/capacity file-swap interruption, automatic-update rollback) as the diagnostic entry point, handing off to all four sibling entries once root-caused. Flags a genuinely more severe risk profile than `WP-ERROR-031`: an interrupted update on an *already-active* plugin can leave the site attempting to load mismatched files on the very next request, a site-wide outage rather than a contained inactive-plugin state. Independent review caught one real finding: `WP-ERROR-013`'s own Common Causes list didn't name an interrupted-update-caused file inconsistency as one of its own downstream-symptom causes — added, cross-referencing this entry. Disclosed as a new `FRAMEWORK-OBSERVATIONS.md` entry: a taxonomy's own independent review verifies every claim it makes about entries it names, but can still miss a claim the artifact never thought to name in the first place.
- **Consistency review** (`SF-REVIEW-094`) treated all three as a system: confirmed the three-stage ownership model holds exactly, and found one Minor finding — `WP-ERROR-017` and `WP-ERROR-031`'s own Related Errors intro sentences used non-standard wording rather than the majority convention 25 of this repository's 28 entries use — corrected.
- **Baseline certification** (`SF-REVIEW-095`) required no correction of its own, since the consistency review had already caught what would otherwise have surfaced here.

Networking is complete and Baseline Certified (`SF-REVIEW-088`) — the sixth category to reach that designation, and the third (after REST API and Authentication) built from a dedicated taxonomy from the outset. Category Work Order: `SF-TAXONOMY-004` (Networking Error Taxonomy), Version 1.4. Entries: `WP-ERROR-028` (Outbound HTTP Request Failure), `WP-ERROR-029` (Outbound TLS Negotiation Failure), `WP-ERROR-030` (CORS Policy Failure), all Production Ready.

- `WP-ERROR-028` (`SF-REVIEW-081`/`082`) — kept deliberately transport-agnostic (`curl`/streams named only as diagnostic detail, never the entry's own definition), separated connection-establishment from protocol (TLS reserved to `WP-ERROR-029`), and explicitly disclosed two conditions this catalog does not yet own under any entry (read/response timeout; an HTTP-level error status from an otherwise-successful outbound exchange) rather than silently absorbing either. Independent review caught a real boundary-precision gap: "connection reset" as originally drafted didn't specify whether it meant a reset during establishment (in scope) or after (out of scope, same as the disclosed read-timeout gap) — qualified to the former.
- `WP-ERROR-029` (`SF-REVIEW-083`/`084`) — `SF-TAXONOMY-004` was widened (v1.0 → v1.2) *before* this entry was authored, per explicit direction: the original scope ("Certificate Verification Failure") was narrower than the category needs, since protocol/cipher negotiation failures aren't certificate problems at all. Retitled "Outbound TLS Negotiation Failure," now covering eight explicitly separated causes rather than blending them. Independent review found a real cross-category overlap: `WP-ERROR-014`'s own diagnosis text already names "a curl build without a specific SSL backend or protocol" as its own territory, which sounds identical to this entry's protocol/cipher causes. Resolved by scope, not mechanism — `WP-ERROR-014` owns a categorical, environment-wide capability gap; this entry owns the observable, request-specific negotiation failure, escalating to `WP-ERROR-014` only once root-caused that deep.
- `WP-ERROR-030` (`SF-REVIEW-085`/`086`) — matched `SF-TAXONOMY-004`'s already-declared Version 1.3 scope exactly, requiring no widening. Reverses WordPress's own role from client (`028`/`029`) to server: CORS is enforced by the browser, WordPress only emits or omits the relevant headers. Carries the two-directional relationship with `WP-ERROR-022` explicitly (a `200 OK` REST response can still be CORS-blocked; a correct CORS policy doesn't override an auth/authz denial). Independent review caught a real internal-precision gap: the entry's own Section 3 claimed WordPress "never becomes aware" a request was cross-origin, which overstated the boundary against the entry's own Section 8 documentation of the origin-aware header-emission mechanism (`get_http_origin()`/`is_allowed_http_origin()`) — narrowed to "never rejects or blocks on the basis of origin, though it does read the Origin header to decide what to emit." This entry's own creation also required updating `WP-ERROR-021`/`022`'s pre-existing CORS-exclusion bullets (previously citing only `SF-TAXONOMY-002`'s forward-reference promise) to cite this entry by a real link — the same class of stale-hedge cleanup `SF-REVIEW-075` performed during the Authentication phase.
- **Consistency review** (`SF-REVIEW-087`) treated all three as a system: confirmed the two-axis ownership model (`028`/`029` sequential, `030` independent) holds exactly, and found two Minor cross-document staleness artifacts of sequential authoring — `WP-ERROR-028`'s own Section 6 still cited `WP-ERROR-029` by its pre-widening title, and Section 16 still carried a stale "(currently Draft)" parenthetical predating `WP-ERROR-029`'s own promotion — both corrected. Disclosed as a second `FRAMEWORK-OBSERVATIONS.md` data point for the `SF-SPEC-013` Section 5.7 staleness family: a sibling entry's own prose, not a placeholder citation, drifting after a retitle/promotion — outside `scripts/validate-repo.sh` Check A's current scope.
- **Baseline certification** (`SF-REVIEW-088`) required no correction of its own, since the consistency review had already caught what would otherwise have surfaced here.

Authentication is complete and Baseline Certified (`SF-REVIEW-079`) — the fifth category to reach that designation, and the second (after REST API) built from a dedicated taxonomy from the outset. Category Work Order: `SF-TAXONOMY-003` (Authentication Error Taxonomy), Version 1.5. Entries: `WP-ERROR-024` (Login Authentication Failure), `WP-ERROR-025` (Authentication Cookie Invalid or Expired), `WP-ERROR-026` (Capability or Role Authorization Denied), `WP-ERROR-027` (Nonce Verification Failure, Non-REST), all Production Ready.

- `WP-ERROR-024` (`SF-REVIEW-070`/`071`) — the first entry in the category and the first authored entirely after `SF-BASELINE-001`. Its independent review caught a real internal-consistency gap in the entry's own lockout-plugin exclusion; its author review caught and fixed a bug in `scripts/validate-repo.sh` itself (Check B was flagging a false-positive "Planned vs. Draft" mismatch; it now only flags once an entry reaches Production Ready, matching established taxonomy-update convention).
- `WP-ERROR-025` (`SF-REVIEW-072`/`073`) — its independent review caught a cross-document attribution error: the entry claimed a boundary case (browser with cookies entirely disabled) belonged to `WP-ERROR-024`, a claim `WP-ERROR-024`'s own text never made and that, on independent technical review, was likely misattributed (credential verification succeeds in that scenario; only cookie persistence fails). Reattributed to `WP-ERROR-025`'s own domain.
- `WP-ERROR-026` (`SF-REVIEW-074`/`075`) — capability-centered per explicit direction, not role-centered; six causes kept deliberately separate; "start broad" diagnostic ordering; explicit prohibition on "make them Administrator" as a default fix. Its independent review found something outside the entry's own file: `WP-ERROR-022` and `SF-TAXONOMY-002` (both already Production Ready/Frozen, authored before this category existed) still carried a generic "Authentication category, once a taxonomy exists for it" hedge — stale now, but invisible to `scripts/validate-repo.sh`, which only matches ID-specific conceptual-reference citations, not generic category-level hedges. Corrected both.
- `WP-ERROR-027` (`SF-REVIEW-076`/`077`) — leads with "a nonce is not authentication, authorization, or replay prevention" per explicit direction; nine causes kept separate; diagnosis pairs generation against verification (action, field, user context, timing, transport) rather than inspecting either side alone; explicit prohibition on disabling nonce verification. Independent review found no new defect — an honest all-Conforming outcome, the category's fourth review and the first with nothing further to correct.
- **Consistency review** (`SF-REVIEW-078`) treated all four as a system: found `WP-ERROR-025`'s Diagnosis lacked the REST-context ruling-out step its siblings already had, and that the specifically-flagged `WP-ERROR-024`-cookies-disabled and `WP-ERROR-025`/`WP-ERROR-027` nonce-before-session-change overlaps were correctly implemented but not stated as explicit rules — added one to `SF-TAXONOMY-003` Section 4. Also tested whether the `WP-ERROR-022`/`SF-TAXONOMY-002` stale-hedge defect (`SF-REVIEW-075`) recurred anywhere else in the category: it did not — the five other similar-looking matches all correctly reference the still-taxonomy-less Security category. Logged as a negative result in `FRAMEWORK-OBSERVATIONS.md`, not yet enough evidence to extend the validator.
- **Baseline certification** (`SF-REVIEW-079`) required no correction of its own, since the consistency review had already caught what would otherwise have surfaced here.

---

## 9. Methodology Documentation

With the candidate-category roadmap complete (Section 3) and the project owner explicitly redirecting future work toward correctness/maintenance rather than fresh taxonomy production, the project owner identified a genuine documentation gap: no single artifact narrated the full category production lifecycle from candidate identification through baseline certification, or the practices this project developed beyond what `SF-SPEC-013` itself formally requires.

**`SF-METHODOLOGY-001` — Knowledge Production Lifecycle** (2026-07-15) closes that gap. It is a new artifact type, distinct from `SF-SPEC-XXX`/`SF-TAXONOMY-XXX`/`WP-ERROR-XXX`: an engineering-methodology document that introduces no new normative requirement of its own, narrating the ten-stage lifecycle `SF-SPEC-013` and `SF-SPEC-012` already govern, each stage grounded in a specific citation to this project's own history rather than asserted in the abstract. It also explicitly discloses seven emergent practices — most consequentially, the proactive cross-category ownership sweep (Section 3.2/5.1 of that document) — that this project has applied without exception since Plugin Lifecycle's own mid-production correction, but which `SF-SPEC-013` itself does not yet formally require. Whether to fold that practice into `SF-SPEC-013` as a normative requirement is named as an open question the new document surfaces but does not resolve.

Reviewed per this project's own established discipline despite not being governed by `SF-SPEC-005`: `SF-REVIEW-147` (Class A author review, one Minor Markdown-formatting defect found and corrected) and `SF-REVIEW-148` (Class B independent review, every citation independently re-derived from its own primary source before comparison with `SF-REVIEW-147`'s findings — zero discrepancy, zero further defect). Both approved with zero outstanding findings.

This does not expand the `WP-ERROR` catalog and certifies no category. It is documentation of the process that produced the fourteen categories already Baseline Certified (Section 1), intended for a future practitioner or a future return to this project to reconstruct the reasoning without re-deriving it from the underlying review records.

---

## 10. Reference Implementation Track

Per the project owner's own priority order (Section 9 above having closed out the methodology-documentation track first), **Reference Implementation** is the second of four future-work tracks: building reproducible runtime evidence per `WP-ERROR` entry, moving entries from reasoned-but-unverified knowledge toward runtime-verified knowledge.

**Governance discovery before any work began:** the term "Reference Implementation" already has a distinct, pre-existing meaning in `SF-SPEC-001` Section 22 (designating an exemplary already-written entry as a model for other authors — informative, not evidence-based). This track is a different concept, closer to the existing `WP-SCENARIO-XXX` runtime-scenario machinery (`SF-SPEC-002`/`003`/`007`), which already governs deterministic, evidence-backed execution — but that machinery is built for a different portfolio (demonstrating the broader SquirrelForge agent's own software-engineering capabilities against a real "Hospital WordPress installation," per `38_WORDPRESS/AGENT-READINESS-REPORT.md`) and carries lifecycle machinery (`SF-SPEC-007`'s eleven-state scenario lifecycle, Distinct Contribution Review, Portfolio Integration) not suited to WP-ERROR-scoped verification. Resolved: the two catalogs (`WP-ERROR-XXX` here, and `38_WORDPRESS/KNOWLEDGE/ERROR-CODES.md`'s separate `WP-PHP-XXX` series) are confirmed intentionally separate by the project owner — no reconciliation needed. This track uses a disposable, self-controlled local environment, never the Hospital installation.

**Artifact format:** a new identifier series, `WP-VERIFICATION-XXX`, reusing the existing `SF-TEMPLATE-005` (Runtime Evidence Record Template) directly — that template's own Section 2 already generalizes to "`WP-SCENARIO-XXX` or other artifact," so no new template needed drafting. Records live in `docs/knowledge/verifications/`. Each record is governed by the same `SF-SPEC-002`/`006`/`011` machinery `WP-SCENARIO-XXX` evidence already uses, without adopting `SF-SPEC-003`/`007`'s scenario-lifecycle/portfolio apparatus.

**Pilot (2026-07-15):** `WP-VERIFICATION-001` verified `WP-ERROR-037` (Upload File Type Rejected) against a disposable local WordPress 7.0.1 installation (WordPress core + the official SQLite Database Integration drop-in + WP-CLI, built entirely in session scratchpad, nothing committed except the evidence record itself). Result: the entry's documented *mechanism* (the two-cause partition, the designed-rejection framing) was confirmed accurate, but two implementation-level details were not — the documented WordPress rejection message does not exist anywhere in WordPress core 7.0.1, and the `unfiltered_upload` capability's actual grant mechanism (`ALLOW_UNFILTERED_UPLOADS`) was entirely undocumented, with only its revocation (`DISALLOW_UNFILTERED_UPLOADS`) covered. A third, taxonomy-level inaccuracy (`SF-TAXONOMY-007` citing a nonexistent filter, `allow_unfiltered_uploads`) was found in the same pass.

All three were corrected through `SF-SPEC-013` Section 5.6's post-certification change process — the first time that process has been fully executed in this catalog (the earlier Filesystem post-certification research, Section 3 above, concluded negatively and never reached the correction steps): `WP-ERROR-037` Version 1.0 → 1.1 (`SF-REVIEW-149` author, `SF-REVIEW-150` independent, both re-deriving every claim from primary WordPress source rather than trusting the verification record's own account), `SF-TAXONOMY-007` Version 1.3 → 1.4, a new Media category consistency review (`SF-REVIEW-151`), and **Media Knowledge Baseline v2** (`SF-REVIEW-152`) — the first `v2` category baseline in this catalog. Logged as a new `FRAMEWORK-OBSERVATIONS.md` entry: a genuinely new category of post-certification-change trigger (runtime evidence surfacing an implementation-level inaccuracy), distinct from the taxonomy-boundary-driven changes `SF-SPEC-013` Section 5.6 was originally modeled on.

**Series convention formalized (2026-07-15):** per the project owner's own explicit request after reviewing the pilot, `docs/knowledge/verifications/README.md` now defines a consistent required-content checklist every `WP-VERIFICATION-XXX` record follows, within `SF-TEMPLATE-005`'s own existing structure (no new template) — explicit "Expected behavior" (Section 3), "Differences from documentation," and "Required repository changes" (both Section 8) sub-points, alongside environment/version/trigger/evidence/negative-validation/cleanup content the template already required. `WP-VERIFICATION-001` retrofitted to this convention (structural only, no factual change).

**Prioritization (2026-07-15, project owner direction):** continue by entries most likely to surface hidden assumptions through observable, deterministic runtime behavior, not in numerical order: Media (remaining: `WP-ERROR-036`, `WP-ERROR-038`) → Filesystem → REST API → Authentication → Networking → Performance → Database → Bootstrap/Configuration.

**`WP-ERROR-038` verified (2026-07-15):** `WP-VERIFICATION-002` runtime-tested Cause 1 (corrupt source image data) against the same disposable environment. Confirmed accurate in every respect this record tested — attachment and original file retained, metadata generation failed silently with no PHP fatal error, exactly as documented. Independently re-executed with a structurally distinct fixture during review (`SF-REVIEW-153`/`154`) for a third confirming execution overall. No correction required; Media remains at Knowledge Baseline v2 (unchanged by a clean confirmation).

**`WP-ERROR-036` verified (2026-07-15) — the most significant Reference Implementation finding to date:** required a real HTTP server (a `php -S` instance plus a disposable receiver script) rather than WP-CLI sideload, since its two PHP-level causes (`post_max_size`, `upload_max_filesize`) are HTTP-request-time behaviors WP-CLI's own CLI sideload path does not exercise. `WP-VERIFICATION-003` confirmed Causes 1 and 2 fully accurate, including exact message and PHP error-log text. Cause 3 was found **substantially inaccurate**: the entry described a generic, single-site-applicable `wp_max_upload_size()`/`upload_size_limit`-filter enforcement mechanism that does not exist in WordPress core — that function/filter pair is used only to render UI display text, confirmed via a live HTTP trigger (accepted a file over the filtered limit) and via direct source verification (`wp_max_upload_size()` called in exactly one place in core, purely for display). The real mechanism, `check_upload_size()` (checking `fileupload_maxk` directly), is registered only on a multisite installation. Corrected via `SF-SPEC-013` Section 5.6 across nine sections of the entry (`SF-REVIEW-155`/`156`), with a new Media consistency review (`SF-REVIEW-157`) and re-certification (`SF-REVIEW-158`, **Media Knowledge Baseline v3**) — Media is now the only category to have undergone the post-certification change process twice. Logged as a second `FRAMEWORK-OBSERVATIONS.md` data point for this trigger class.

**`WP-ERROR-019` verified (2026-07-16):** `WP-VERIFICATION-004` confirmed direct-write denial, ancestor-traversal denial, upload behavior, installer staging, restoration, and repeatability against WordPress 7.0.1. One literal installer quotation had incorrect capitalization/interface attribution; it was corrected through `SF-REVIEW-159`–`167`, producing Filesystem Knowledge Baseline v2 and Plugin Knowledge Baseline v2.

**`WP-ERROR-020` verified (2026-07-16):** `WP-VERIFICATION-005` used a disposable 96 MiB HFS+ volume to produce genuine zero-free-block capacity exhaustion. Direct PHP writes, Site Health, REST media upload, plugin staging, permission-negative validation, recovery, and cleanup matched the entry. The Mac reference environment could not faithfully exhaust inodes or enforce quota, so those mechanisms remain explicitly source-verified/platform-deferred. Research exposed one stale cross-category statement inherited from before `WP-VERIFICATION-003`: `wp_max_upload_size()` was still described as enforcement. Verification paused; the claim was corrected separately in commit `961c19f` through `SF-REVIEW-169`–`174`, producing Filesystem Knowledge Baseline v3 and Media Knowledge Baseline v4; verification then completed via `SF-REVIEW-175`/`176`.

**`WP-ERROR-016` verified (2026-07-16):** `WP-VERIFICATION-006` confirmed against WordPress 7.0.1 and WP-CLI 2.12.0 that `wp core verify-checksums` runs before WordPress loads, continues to operate when `wp-settings.php` makes normal bootstrap fail, and reports missing, altered, and unreadable core files. Network retrieval, version/locale metadata, installation location, filesystem readability, and WP-CLI execution were separately isolated as genuine environmental dependencies rather than bootstrap requirements. A forced exact-version core download restored both clean checksums and normal bootstrap. No discrepancy was found against corrected WP-ERROR-016 Version 1.1; reviews `SF-REVIEW-181`/`182` approved the record with no knowledge change.

**`WP-ERROR-021` verified (2026-07-16):** after the source gate corrected route/rewrite ownership separately in commit `da51e91`, `WP-VERIFICATION-007` confirmed against WordPress 7.0.1 that missing path, namespace, unsupported method, and `rest_endpoints` removal produce exact `rest_no_route` HTTP 404 responses after REST dispatch. Permission denial and callback failure remained distinct downstream controls. Query-string success during an explicit pre-WordPress pretty-path interception demonstrated the generic-404 boundary. Core WP-CLI had no `rest` command while `wp eval` inspected the route table successfully. Apache/Nginx/WAF execution remains explicitly deferred. No discrepancy was found against WP-ERROR-021 Version 1.1; reviews `SF-REVIEW-187`/`188` approved the record with no knowledge change.

**`WP-ERROR-022` verified (2026-07-17):** after the source gate corrected missing-versus-invalid nonce behavior, Application Password availability, and authentication-mechanism paths separately in commit `268cdc3`, `WP-VERIFICATION-008` confirmed against WordPress 7.0.1 that a missing nonce leaves cookie requests anonymous while an invalid supplied nonce returns exact `rest_cookie_invalid_nonce` HTTP 403. Permission denial produced 401 for anonymous and 403 for an authenticated subscriber when the route deliberately used `rest_authorization_required_code()`. Application Password success, request-scoped unavailability, invalidity, and revocation were exercised; the three denial states shared an external anonymous 401 and remained distinguishable through configuration and credential state. Argument validation and `rest_pre_dispatch` denial prevented the main callback, while route and callback errors remained adjacent controls. Third-party authentication and natural nonce expiry remain explicitly source-verified. No discrepancy was found against Version 1.1; reviews `SF-REVIEW-193`/`194` approved the record with no knowledge change.

**`WP-ERROR-023` verified (2026-07-17):** `WP-VERIFICATION-009` verified callback-supplied `WP_Error`, uncaught-exception response disruption, Core's `rest_encode_error` HTTP 500 path for a returned resource, and a configuration-dependent response-corruption path in which displayed PHP warning output preceded an otherwise-valid JSON payload. Pre-dispatch denial, anonymous authorization denial, and missing route controls established the WP-ERROR-022 and WP-ERROR-021 boundaries; immutable SQLite snapshot restoration and final healthy controls passed. Runtime evidence found Version 1.1 omitted the output-corruption manifestation. Verification paused while that correction completed separately in `64bd901`, merged through `82da424`, with `SF-REVIEW-204`–`207` re-certifying REST API Knowledge Baseline v5. The final record evaluates the evidence against WP-ERROR-023 Version 1.2; Apache, Nginx, PHP-FPM, and alternate display-error configurations remain unexecuted. Reviews `SF-REVIEW-208`/`209` approved the record with no further knowledge change.

**`WP-ERROR-024` verified (2026-07-19):** `WP-VERIFICATION-010` confirmed ordinary empty-field validation, credential-rejection, and valid XML-RPC authentication behavior against WordPress 7.0.1. Browser-session persistence/logout and REST Application Password authentication were retained as adjacent `WP-ERROR-025` and `WP-ERROR-022` boundary controls rather than target-condition evidence. Environment qualification for REST Application Password availability was separately recorded as setup diagnostics. Case reviews and report reviews `SF-REVIEW-214`/`215` found no contradiction with WP-ERROR-024 Version 1.1 and required no knowledge change.

**`WP-ERROR-028` networking boundaries verified (2026-07-19):** `WP-VERIFICATION-011` exercised connection refusal, DNS resolution failure, and TCP connection-establishment timeout against WordPress 7.0.1, each producing the WordPress-facing `http_request_failed` transport error without an HTTP response. A proposed reset-during-establishment trigger was not operationally distinct from refusal on the macOS reference environment and was excluded as a duplicate. The TLS-negotiation case crossed the TCP boundary and was therefore classified under `WP-ERROR-029`; the HTTP 404 control demonstrated a completed transport with an error response rather than a transport failure. All accepted cases were independently reviewed with documented platform and evidence limits; no knowledge or taxonomy change was required.

**Status:** all three Media entries, all three Filesystem entries, all three REST API entries, and the Networking boundaries covered by `WP-VERIFICATION-011` now have verification records. The campaign is closed; the next category follows the approved Reference Implementation priority order.

**`WP-ERROR-033` caching/performance boundaries verified (2026-07-23):** `WP-VERIFICATION-012` exercised Redis backend unavailability during drop-in bootstrap, a connected-backend write-operation denial, and a controlled drop-in initialization failure against WordPress 7.0.1 with Redis Object Cache 2.8.0 and Predis 2.4.0. All cases were independently reviewed with documented implementation and platform limitations; no knowledge or taxonomy change was required. The campaign is closed.

**`WP-ERROR-034` page-cache boundaries verified (2026-07-23):** `WP-VERIFICATION-013` exercised missing drop-in engagement, an administrative no-op, and cache-write failure against WordPress 7.0.1 using a deterministic disposable page-cache fixture. All cases were independently reviewed with documented fixture and filesystem limitations; no knowledge or taxonomy change was required. The campaign is closed.

**`WP-ERROR-035` OPcache stale-bytecode boundaries verified (2026-07-24):** `WP-VERIFICATION-014` exercised disabled timestamp validation, timed timestamp revalidation, and the distinction between stale web-worker bytecode and fresh CLI execution against WordPress 7.0.1 with Apache/PHP-FPM. All cases were independently reviewed with documented pool, configuration, CLI, and platform limitations; no knowledge or taxonomy change was required. The campaign is closed.

**`WP-ERROR-007` database connection-limit boundary verified (2026-07-24):** `WP-VERIFICATION-015` exercised database authentication rejection, missing database, query permission denial, and controlled MariaDB connection-limit exhaustion against WordPress 7.0.1. All cases were independently reviewed with documented implementation and evidence limitations; no knowledge or taxonomy change was required. The campaign is closed.

**`WP-ERROR-008` database server unreachability boundaries verified (2026-07-24):** `WP-VERIFICATION-016` exercised connection refusal, DNS resolution failure, and TCP connection-establishment timeout against WordPress 7.0.1. All cases were independently reviewed with documented platform and evidence limitations; no knowledge or taxonomy change was required. The campaign is closed.
